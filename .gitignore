# Local workspace artifacts
.workspace/
.opencode/
result
result/
result-*

# Go build artifacts
unimart
dist/
vendor/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Nix
.direnv/

# Secrets — never commit
*.pem
*.key
credentials.json
.env
.env.*
!.env.example
packages/backstage/templates/secret.yaml
repositories/backstage-platform/
repositories/backstage-templates/

# Build-time copies of cmdr configs (populated by BuildSandbox before docker build)
containers/sandbox/hm/

# Pending submodule registration (needs GitHub repo + gh auth)
/docs-service/

# idpbuilder runtime artifacts (Gitea workflows written into the tree)
idpbuilder/.gitea/

# Pull-only clones of platform-generated repos (populated by scripts/mirror-platform-repos.sh)
mirrors/
