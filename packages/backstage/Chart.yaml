apiVersion: v2
name: backstage-platform
description: Backstage IDP with full platform integration (Gitea, ArgoCD, Kubernetes)
type: application
version: 1.0.0
appVersion: "1.33.0"

dependencies:
  - name: backstage
    version: 2.8.2
    repository: https://backstage.github.io/charts
